export enum BASE_TAGS {
  ERROR = 'Error',
  PROFILE = 'Profile',
  TICKETS = 'Tickets',
  TICKET = 'Ticket',
  USER_TICKETS = 'UserTickets',
}

export const BASE_TAGS_ARRAY = Object.values(BASE_TAGS);
